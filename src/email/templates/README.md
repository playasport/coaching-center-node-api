## Email Templates

Store reusable HTML templates in this directory. Templates support simple token
replacement—use placeholders like `{{otp}}` which will be replaced at runtime.

Add new templates as needed and reference them from the email service.



